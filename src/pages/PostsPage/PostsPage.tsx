import {FC} from "react";

import {Posts} from "../../components/Posts/Posts.tsx";

const PostsPage: FC = () => {
    return (
        <div>
            <Posts/>
        </div>
    );
};

export {PostsPage};